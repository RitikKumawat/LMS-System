query GetCourseById($courseId: String!) {
  getCourseById(courseId: $courseId) {
    _id
    title
    slug
    description
    thumbnail_url
    category_id
    level
    language
    price
    is_published
    created_by
    createdAt
    updatedAt
  }
}